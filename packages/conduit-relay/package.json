{
  "name": "conduit-relay",
  "version": "0.0.0",
  "description": "conduit (Discord Gateway <-> Cloudflare Workers relay) as a native Node.js addon, runnable via npx or as a library.",
  "type": "module",
  "bin": {
    "conduit-relay": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "lint": "biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/bun": "^1",
    "node-api-headers": "^1.9.0",
    "typescript": "^6.0.3"
  },
  "packageManager": "bun@1.3.14"
}
